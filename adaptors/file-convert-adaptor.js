'use strict';

/**
 * Convert an array of files to another type.
 */
module.exports = (files, { type = 'text' }) => {
    console.log(`Converting these files to type ${type}...`, files);

    // TODO: Implement
};
